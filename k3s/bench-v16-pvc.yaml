apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bench-v16-data
  namespace: frappe-v16
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
